/**
 * Steht in der Artenauswahl eines Kalender-Abos für die Sitzungen. Sie haben
 * keine Veranstaltungsart, erscheinen in der Auswahl aber neben den Arten — und
 * eine UUID kann nie so lauten, also bleibt die Auswahl eindeutig.
 */
export const calendarSessionKind = 'session'
